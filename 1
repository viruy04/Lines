#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    printf("Введите строку: ");
    scanf("%s", str);

    int len = strlen(str);

    printf("%s, %s, %s\n", str, str, str);
    printf("Количество символов в строке: %d\n", len);

    return 0;
}
